# --- Auth (GitHub OAuth app: callback URL = <origin>/api/auth/callback/github) ---
AUTH_SECRET=generate-with-npx-auth-secret
AUTH_GITHUB_ID=your-github-oauth-client-id
AUTH_GITHUB_SECRET=your-github-oauth-client-secret

# --- Model providers (set the ones you have; models auto-enable per key) ---
# Cerebras — ultra-fast inference (Gemma 4 31B, GPT-OSS 120B)
CEREBRAS_API_KEY=
# Groq — very low latency (Llama 3.1 8B Instant, Llama 3.3 70B)
GROQ_API_KEY=
# Google AI Studio — Gemini 3.1 Flash Lite, Gemini 2.5 Flash
GOOGLE_AI_API_KEY=
# OpenAI — GPT-4o mini translation, plus server-side Whisper STT and TTS dubbing
OPENAI_API_KEY=

# Optional: where the SQLite database lives (defaults to ./data/loqui.db)
# LOQUI_DB_PATH=./data/loqui.db
